# Environment Variables
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
.venv/
env/
ENV/
env.bak/
venv.bak/

# Node / frontend build (dashboard SPA — rebuilt in Docker)
node_modules/
dashboard/frontend/dist/
*.tsbuildinfo

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs (runtime output only - do NOT use a bare `log/`, it matches the
# vendored `storage/log/` source package and silently drops it from the repo)
logs/
*.log

# Database
*.db
*.sqlite
*.sqlite3

# Docker
docker-compose.override.yml

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# MyPy
.mypy_cache/
.dmypy.json
dmypy.json

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# Backup files
*.bak
*.backup
*.tmp

# OS
Thumbs.db
desktop.ini
/docs/

# Local-only docker test files (build from working tree; never deployed)
docker/Dockerfile.local
docker/Dockerfile.dashboard.local
docker/docker-compose.local.yml
/dashboard/.claude/
